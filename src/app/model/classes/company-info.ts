export class CompanyInfo {
    constructor(public id: number
        , public name: string,
        public logo: string, public services: string[], public cabital: number, public establishDate: Date) { }
}
